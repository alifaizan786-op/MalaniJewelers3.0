<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Order Confirmation</title>
  </head>
  <body style="margin: 0; padding: 0; font-family: 'Abhaya Libre', serif; background-color: #f5f5f5; color: #333;">
    <section style="padding: 2rem 0">
      <!-- HEADER -->
      <table style="width: 90%; max-width: 600px; margin: 0 auto; background: #fff; border-radius: 15px 15px 0 0; box-shadow: 0 0 15px rgba(0, 0, 0, 0.1);">
        <tr align="center">
          <td style="padding: 2rem">
            <img src="{{ 'logo_email.svg' | shopify_asset_url }}" alt="{{ shop.name }} Logo" style="width: 100%; max-width: 700px; height: auto" />
          </td>
        </tr>
        <tr align="center">
          <td style="padding: 1rem 0">
            <svg width="150" height="150" viewBox="0 0 200 200">
              <style>
                .circle { stroke-dasharray: 570; stroke-dashoffset: 0; stroke: #212121; fill: none; }
                .check { stroke-dasharray: 120; stroke-dashoffset: 0; stroke: #cfa96b; fill: none; }
              </style>
              <circle cx="100" cy="100" r="90" stroke-width="10" class="circle" />
              <polyline points="70,100 95,125 135,85" stroke-width="10" stroke-linecap="round" stroke-linejoin="round" class="check" />
            </svg>
          </td>
        </tr>
      </table>

      <!-- ORDER MESSAGE -->
      <table style="width: 90%; max-width: 600px; margin: 0 auto; background: #fff;">
        <tr align="center">
          <td style="padding: 1rem">
            <h1 style="color: #cfa96b; font-size: 2.5rem; margin: 0">Order Received!</h1>
            <p style="font-size: 1.25rem; margin: 0.5rem 0">Thank you for your order!</p>
            <p style="font-size: 1.1rem; margin: 0.5rem 2rem">
              Your order <strong>#{{ name }}</strong> is being processed. We’ll notify you once it ships.
            </p>
          </td>
        </tr>
      </table>

      <!-- BILL TO / SHIP TO -->
      <table style="width: 90%; max-width: 600px; margin: 0 auto; background: #fff;">
        <tr>
          <td style="padding: 1rem 2rem;">
            <h3 style="color: #cfa96b;">Bill To:</h3>
            <p>
              {{ billing_address.name }}<br />
              {{ billing_address.address1 }}<br />
              {% if billing_address.address2 != blank %}{{ billing_address.address2 }}<br />{% endif %}
              {{ billing_address.city }}, {{ billing_address.province_code }} {{ billing_address.zip }}
            </p>
          </td>
          <td style="padding: 1rem 2rem;">
            <h3 style="color: #cfa96b;">Ship To:</h3>
            <p>
              {{ shipping_address.name }}<br />
              {{ shipping_address.address1 }}<br />
              {% if shipping_address.address2 != blank %}{{ shipping_address.address2 }}<br />{% endif %}
              {{ shipping_address.city }}, {{ shipping_address.province_code }} {{ shipping_address.zip }}
            </p>
          </td>
        </tr>
      </table>

      <!-- ITEMS -->
      <table style="width: 90%; max-width: 600px; margin: 0 auto; background: #fff;">
        <tr>
          <td colspan="2" style="padding: 1rem 2rem;">
            <h2 style="color: #cfa96b; margin: 0">Your Order Includes:</h2>
          </td>
        </tr>
        {% for line in line_items %}
        <tr>
          <td style="padding: 1rem; text-align: center">
            {% if line.image %}
              <img src="{{ line.image | img_url: '300x' }}" style="border-radius: 8px; max-width: 100%; height: auto;" />
            {% endif %}
          </td>
          <td style="padding: 1rem 2rem">
            <p><strong>SKU:</strong> {{ line.sku }}</p>
            <p><strong>Product:</strong> {{ line.title }}</p>
            <p><strong>QTY:</strong> {{ line.quantity }}</p>
            <p><strong>Price:</strong> {{ line.line_price | money }}</p>
          </td>
        </tr>
        {% endfor %}
      </table>

      <!-- TOTALS -->
      <table style="width: 90%; max-width: 600px; margin: 0 auto; background: #fff; font-size: 1.25rem;">
        <tr>
          <td style="padding: 1rem 2rem">Subtotal:</td>
          <td style="padding: 1rem 2rem; text-align: right">{{ subtotal_price | money }}</td>
        </tr>
        <tr>
          <td style="padding: 1rem 2rem">Shipping:</td>
          <td style="padding: 1rem 2rem; text-align: right">{{ shipping_price | money }}</td>
        </tr>
        <tr>
          <td style="padding: 1rem 2rem">Tax:</td>
          <td style="padding: 1rem 2rem; text-align: right">{{ tax_price | money }}</td>
        </tr>
        <tr>
          <td style="padding: 1rem 2rem"><strong>Total:</strong></td>
          <td style="padding: 1rem 2rem; text-align: right"><strong>{{ total_price | money }}</strong></td>
        </tr>
      </table>

      <!-- SUPPORT -->
      <table style="width: 90%; max-width: 600px; margin: 0 auto; background: #cfa96b; color: #fff;">
        <tr>
          <td style="padding: 2rem">
            <h3>Questions?</h3>
            <p>Visit <a style="color: white; text-decoration: underline;" href="{{ shop.url }}/pages/shipping">Shipping & Handling</a></p>
            <p>Call: <a style="color: white; text-decoration: underline;" href="tel:18880006666">+1 (888) 000-6666</a></p>
            <p>Email: <a style="color: white; text-decoration: underline;" href="mailto:info@malanijewelers.com">info@malanijewelers.com</a></p>
          </td>
        </tr>
      </table>

      <!-- MORE FROM -->
      <table style="width: 90%; max-width: 600px; margin: 0 auto; background: #fff; text-align: center;">
        <tr>
          <td colspan="3">
            <h2 style="padding: 2rem 0">More From Your Forever Jeweler</h2>
          </td>
        </tr>
        <tr>
          <td style="padding: 1rem;">
            <a href="{{ shop.url }}/collections/womens-new-arrivals" style="background: #cfa96b; color: #fff; padding: 1rem 2rem; border-radius: 5px; text-decoration: none; display: inline-block;">Women's New Arrivals</a>
          </td>
          <td style="padding: 1rem;">
            <a href="{{ shop.url }}/collections/mens-new-arrivals" style="background: #cfa96b; color: #fff; padding: 1rem 2rem; border-radius: 5px; text-decoration: none; display: inline-block;">Men's New Arrivals</a>
          </td>
          <td style="padding: 1rem;">
            <a href="{{ shop.url }}/collections/kids" style="background: #cfa96b; color: #fff; padding: 1rem 2rem; border-radius: 5px; text-decoration: none; display: inline-block;">Kids' Picks</a>
          </td>
        </tr>
        <tr>
          <td colspan="3">
            <h3 style="padding-top: 2rem">Free Shipping on Orders $750+</h3>
          </td>
        </tr>
        <tr>
          <td colspan="3">
            <h3 style="padding-top: 2rem">
              Buy Now, Pay Later with
              <a href="{{ shop.url }}/pages/affirm" style="color: #cfa96b;">Affirm</a>
            </h3>
          </td>
        </tr>
      </table>
    </section>
  </body>
  <script type="application/ld+json">
{
  "@context": "http://schema.org",
  "@type": "Order",
  "merchant": {
    "@type": "Organization",
    "name": "Malani Jewelers",
    "url": "https://www.malanijewelers.com"
  },
  "orderNumber": "{{ name }}",
  "orderStatus": "http://schema.org/OrderProcessing",
  "priceCurrency": "{{ currency }}",
  "price": "{{ total_price | money_without_currency }}",
  "acceptedOffer": [
    {% for line in line_items %}
    {
      "@type": "Offer",
      "itemOffered": {
        "@type": "Product",
        "name": "{{ line.title }}",
        "sku": "{{ line.sku }}",
        "image": "{{ line.image | img_url: '400x' }}"
      },
      "price": "{{ line.price | money_without_currency }}",
      "priceCurrency": "{{ currency }}",
      "eligibleQuantity": {
        "@type": "QuantitativeValue",
        "value": "{{ line.quantity }}"
      }
    }{% unless forloop.last %},{% endunless %}
    {% endfor %}
  ],
  "orderDelivery": {
    "@type": "ParcelDelivery",
    "deliveryAddress": {
      "@type": "PostalAddress",
      "name": "{{ shipping_address.name }}",
      "streetAddress": "{{ shipping_address.address1 }}{% if shipping_address.address2 != blank %}, {{ shipping_address.address2 }}{% endif %}",
      "addressLocality": "{{ shipping_address.city }}",
      "addressRegion": "{{ shipping_address.province_code }}",
      "postalCode": "{{ shipping_address.zip }}",
      "addressCountry": "{{ shipping_address.country_code }}"
    }
  }
}
</script>

</html>
